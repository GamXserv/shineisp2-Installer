CREATE TABLE {prefix}user(
	id INT NOT NULL AUTO_INCREMENT,
	name VARCHAR(100) NOT NULL,
	pass VARCHAR(100) NOT NULL,
	PRIMARY KEY(id)
);

CREATE TABLE {prefix}color(
	id INT NOT NULL AUTO_INCREMENT,
	name VARCHAR(100) NOT NULL,
	PRIMARY KEY(id)
);